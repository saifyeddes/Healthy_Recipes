@model Healthy_Recipes.ViewModels.LoginViewModel

@{
    ViewData["Title"] = "Connexion";
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
}

<div class="account-container max-w-md w-full bg-white rounded-xl shadow-md p-8">
    <h2 class="text-2xl font-bold text-gray-800 text-center mb-6">Connexion</h2>
    <form asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="space-y-6">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-red-600 text-sm text-center"></div>

        <div>
            <label asp-for="Email" class="block text-sm font-medium text-gray-700 mb-1"></label>
            <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                    <i class="fas fa-envelope text-gray-400"></i>
                </div>
                <input asp-for="Email" class="w-full px-10 py-2 border rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500" placeholder="Votre email" />
            </div>
            <span asp-validation-for="Email" class="text-red-600 text-sm"></span>
        </div>

        <div>
            <label asp-for="Password" class="block text-sm font-medium text-gray-700 mb-1"></label>
            <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                    <i class="fas fa-lock text-gray-400"></i>
                </div>
                <input asp-for="Password" class="w-full px-10 py-2 border rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500" placeholder="Votre mot de passe" />
            </div>
            <span asp-validation-for="Password" class="text-red-600 text-sm"></span>
        </div>

        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <input asp-for="RememberMe" class="h-4 w-4 rounded border-gray-300 text-green-600 focus:ring-green-500" />
                <label asp-for="RememberMe" class="ml-2 block text-sm text-gray-700">Se souvenir de moi</label>
            </div>
            <a asp-action="ForgotPassword" class="text-sm text-green-700 hover:text-green-600">Mot de passe oublié ?</a>
        </div>

        <button type="submit" class="w-full bg-green-700 text-white py-2 rounded-lg font-medium hover:bg-green-800 transition">
            <i class="fas fa-sign-in-alt mr-2"></i>Connexion
        </button>

        <div class="text-center">
            <p class="text-sm text-gray-600">
                Pas encore de compte ?
                <a asp-action="Register" class="text-green-700 hover:underline">S'inscrire</a>
            </p>
            <a asp-controller="Home" asp-action="Index" class="text-green-700 hover:underline text-sm">Retour</a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
}